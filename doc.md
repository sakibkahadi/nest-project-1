/**
steps of crud

1. create a schema and dto for the schema
2. then in module register that schema with mongoose module for feature fucntion which will take an array of object with name and schema
3. then inject the model in servcie using @InjectModel decorator and then use that model to perform crud operation
4. then create a controller and inject the service in controller and then create the routes for crud operation
5. then in module register the controller and service
   */
