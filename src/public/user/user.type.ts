// store the const value of user type

export enum Role {
  ADMIN = 'Admin',
  EMPLOYEE = 'Employee',
}

export enum ProfileModel {
  ADMIN = 'Admin',
  EMPLOYEE = 'Employee',
}

export enum UserStatus {
  ACTIVE = 'Active',
  INACTIVE = 'Inactive',
  BLOCKED = 'Blocked',
  DEACTIVATED = 'Deactivated',
  NOT_VERIFIED = 'Not Verified',
}
